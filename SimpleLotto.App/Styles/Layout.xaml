<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="SlPageTitleTextStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource TitleTextBlockStyle}">
        <Setter Property="Foreground" Value="{ThemeResource SlTextBrush}"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <Style x:Key="SlSectionTitleTextStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource BodyStrongTextBlockStyle}">
        <Setter Property="Foreground" Value="{ThemeResource SlTextBrush}"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <Style x:Key="SlCaptionTextStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource CaptionTextBlockStyle}">
        <Setter Property="Foreground" Value="{ThemeResource SlMutedTextBrush}"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <Style x:Key="SlMetricTextStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource SubtitleTextBlockStyle}">
        <Setter Property="Foreground" Value="{ThemeResource SlTextBrush}"/>
        <Setter Property="TextWrapping" Value="NoWrap"/>
    </Style>

    <x:Double x:Key="SlBinTileSize">52</x:Double>

    <Style x:Key="SlPanelBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource SlSurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{ThemeResource SlBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="{StaticResource ControlCornerRadius}"/>
        <Setter Property="Padding" Value="20"/>
    </Style>

    <Style x:Key="SlHeaderMetricCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource SlSurfaceAltBrush}"/>
        <Setter Property="BorderBrush" Value="{ThemeResource SlBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Padding" Value="8,5"/>
    </Style>

    <Style x:Key="SlHeaderMetricCaptionTextStyle" TargetType="TextBlock" BasedOn="{StaticResource SlCaptionTextStyle}">
        <Setter Property="FontSize" Value="11"/>
    </Style>

    <Style x:Key="SlShiftActionButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{ThemeResource SlShiftActionBrush}"/>
        <Setter Property="BorderBrush" Value="{ThemeResource SlWarningBrush}"/>
        <Setter Property="Foreground" Value="{ThemeResource SlTextBrush}"/>
        <Setter Property="CornerRadius" Value="{StaticResource ControlCornerRadius}"/>
        <Setter Property="MinHeight" Value="34"/>
    </Style>

    <Style x:Key="SlLogoutActionButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{ThemeResource SlLogoutActionBrush}"/>
        <Setter Property="BorderBrush" Value="{ThemeResource SlDangerBrush}"/>
        <Setter Property="Foreground" Value="{ThemeResource SlTextBrush}"/>
        <Setter Property="CornerRadius" Value="{StaticResource ControlCornerRadius}"/>
        <Setter Property="MinHeight" Value="34"/>
    </Style>
</ResourceDictionary>
